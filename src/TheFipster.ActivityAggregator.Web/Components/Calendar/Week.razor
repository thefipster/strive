    <tr>
        @for (int i = 0; i < 7; i++)
        {
            @if ((Index == 1 && Start.AddDays(i).Day > 7) || (Index > 3 && Start.AddDays(i).Day < 7))
            {
                <td class="off-month">
                <Day Date="Start.AddDays(i)" State="GetState(Start.AddDays(i))" />
                </td>
            }
            else
            {
                <td>
                <Day Date="Start.AddDays(i)" State="GetState(Start.AddDays(i))" />
                </td>
            }

        }

        <td>
            <WeekSummary />
        </td>
    </tr>
