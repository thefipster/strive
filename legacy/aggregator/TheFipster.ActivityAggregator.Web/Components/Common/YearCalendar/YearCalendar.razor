    <MudContainer MaxWidth="MaxWidth.False">
        <MudGrid>
        @if (_monthlyInventory.Any())
        {
            @foreach (var month in _monthlyInventory.OrderBy(x => x.Key))
            {
                <MudItem xs="1">
                    <MonthColumn Month="@month.Key" Inventory="@month.Value" Batches="@_monthyBatches[month.Key]"/>
                </MudItem>
            }
        }
        </MudGrid>
    </MudContainer>
