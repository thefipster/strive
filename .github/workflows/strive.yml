name: strive-build

on:
  push:
    branches: [ "main" ]
    paths:
      - 'src/**'
      - 'test/**'
      - 'strive.sln'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - 'NuGet.config'
      - 'global.json'
      - '.github/workflows/strive.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'src/**'
      - 'test/**'
      - 'strive.sln'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - 'NuGet.config'
      - 'global.json'
      - '.github/workflows/strive.yml'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Restore
      run: dotnet restore strive.sln

    - name: Build
      run: dotnet build --no-restore --configuration Release strive.sln

    - name: Test
      run: dotnet test --no-build --configuration Release strive.sln
